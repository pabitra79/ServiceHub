<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Manager Account</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link href="/css/add-manager.css" rel="stylesheet" />
  </head>
  <body>
      <div class="form-header">
        <h2><i class="fas fa-user-tie me-2"></i>Create Manager Account</h2>
        <p>Add a new manager to your organization</p>
      </div>

      <div id="alertSuccess" class="alert alert-success">
        <i class="fas fa-check-circle me-2"></i>Manager account created
        successfully!
      </div>

      <div id="alertError" class="alert alert-error">
        <i class="fas fa-exclamation-circle me-2"></i
        ><span id="errorMessage">Please fix the errors below.</span>
      </div>
            <% if (messages.success && messages.success.length > 0) { %>
        <div class="alert alert-success">
          <i class="fas fa-check-circle me-2"></i><%= messages.success[0] %>
        </div>
      <% } %>

      <% if (messages.error && messages.error.length > 0) { %>
        <div class="alert alert-error">
          <i class="fas fa-exclamation-circle me-2"></i><%= messages.error[0] %>
        </div>
      <% } %>

      <!-- Temporary Password Display -->

      <div class="row">
          <div class="col-md-6">
            <div class="form-group">
              <label for="name">Full Name *</label>
              <input
                type="text"
                id="name"
                name="name"
                class="form-control"
                placeholder="Enter full name"
                value="<%= typeof name !== 'undefined' ? name : '' %>"
                required
              />
              <i class="fas fa-user input-icon"></i>
            </div>
          </div>
          <div class="col-md-6">
            <div class="form-group">
              <label for="phone">Phone Number *</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                class="form-control"
                placeholder="Enter 10-digit phone number"
                value="<%= typeof phone !== 'undefined' ? phone : '' %>"
                maxlength="10"
                required
              />
              <i class="fas fa-phone input-icon"></i>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-md-6">
            <div class="form-group">
              <label for="email">Email Address *</label>
              <input
                type="email"
                id="email"
                name="email"
                class="form-control"
                placeholder="Enter email address"
                value="<%= typeof email !== 'undefined' ? email : '' %>"
                required
              />
              <i class="fas fa-envelope input-icon"></i>
            </div>
          </div>
          <div class="col-md-6">
            <div class="form-group">
              <label for="gender">Gender *</label>
              <select
                id="gender"
                name="gender"
                class="form-control"
                required
              >
                <option value="">Select Gender</option>
                <option value="male" <%= (typeof gender !== 'undefined' && gender === 'male') ? 'selected' : '' %>>Male</option>
                <option value="female" <%= (typeof gender !== 'undefined' && gender === 'female') ? 'selected' : '' %>>Female</option>
                <option value="others" <%= (typeof gender !== 'undefined' && gender === 'others') ? 'selected' : '' %>>Others</option>
              </select>
              <i class="fas fa-venus-mars input-icon"></i>
            </div>
          </div>
        </div>

        <div class="form-group">
          <label for="address">Address *</label>
          <textarea
            id="address"
            name="address"
            class="form-control"
            placeholder="Enter complete address"
            rows="3"
            required
          ><%= typeof address !== 'undefined' ? address : '' %></textarea>
          <i class="fas fa-map-marker-alt input-icon"></i>
        </div>

        <div class="row">
          <div class="col-md-6">
            <div class="form-group">
              <label for="department">Department *</label>
              <select
                id="department"
                name="department"
                class="form-control"
                required
              >
                <option value="">Select Department</option>
                <option value="operations" <%= (typeof department !== 'undefined' && department === 'operations') ? 'selected' : '' %>>Operations</option>
                <option value="sales" <%= (typeof department !== 'undefined' && department === 'sales') ? 'selected' : '' %>>Sales</option>
                <option value="hr" <%= (typeof department !== 'undefined' && department === 'hr') ? 'selected' : '' %>>Human Resources</option>
                <option value="finance" <%= (typeof department !== 'undefined' && department === 'finance') ? 'selected' : '' %>>Finance</option>
                <option value="it" <%= (typeof department !== 'undefined' && department === 'it') ? 'selected' : '' %>>Information Technology</option>
              </select>
              <i class="fas fa-building input-icon"></i>
            </div>
          </div>
          <div class="col-md-6">
            <div class="form-group">
              <label for="userImage">Profile Image</label>
              <input
                type="file"
                id="userImage"
                name="userImage"
                class="form-control"
                accept="image/*"
              />
              <i class="fas fa-camera input-icon"></i>
              <img id="imagePreview" class="image-preview" src="#" alt="Preview">
            </div>
          </div>
        </div>

        <button type="submit" class="btn-submit">
          <i class="fas fa-user-plus me-2"></i>Create Manager Account
        </button>
      </form>

      <div class="form-footer">
        <p>
          <a href="/admin/dashboard" class="me-3">
            <i class="fas fa-arrow-left me-1"></i>Back to Dashboard
          </a>
          <br class="d-md-none">
          By creating an account, you agree to our
          <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>.
        </p>
      </div>

    <script src="/js/add-manager.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
