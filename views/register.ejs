<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link href="/css/navbar.css" rel="stylesheet" />
    <link href="/css/home.css" rel="stylesheet" />
    <link href="/css/register.css" rel="stylesheet" />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('layouts/navbar') %>

    <main class="register-main-container">
      <div class="register-form-container">
        <div class="register-header">
          <h1>Create Account</h1>
          <p>Join Service Management System</p>
        </div>

        <% if (locals.error) { %>
        <div class="register-alert register-alert-error"><%= error %></div>
        <% } %> <% if (locals.success) { %>
        <div class="register-alert register-alert-success"><%= success %></div>
        <% } %>

        <div class="register-info-box">
          <strong>📌 Note:</strong> All new users register as
          <strong>Customers</strong>. If you need access as a Technician or
          Manager, contact your administrator.
        </div>

        <form action="/register" method="POST" enctype="multipart/form-data">
          <div class="register-form-row">
            <div class="register-form-group">
              <label for="name">Full Name</label>
              <input
                type="text"
                id="name"
                name="name"
                placeholder="John Doe"
                required
              />
            </div>
            <div class="register-form-group">
              <label for="email">Email Address</label>
              <input
                type="email"
                id="email"
                name="email"
                placeholder="john@example.com"
                required
              />
            </div>
          </div>

          <div class="register-form-row">
            <div class="register-form-group">
              <label for="gender">Gender</label>
              <select id="gender" name="gender" required>
                <option value="">Select Gender</option>
                <option value="male">Male</option>
                <option value="female">Female</option>
                <option value="others">Others</option>
              </select>
            </div>
            <div class="register-form-group">
              <label for="phone">Phone Number</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                placeholder="9876543210"
                maxlength="10"
                required
              />
            </div>
          </div>

          <div class="register-form-group">
            <label for="address">Address</label>
            <input
              type="text"
              id="address"
              name="address"
              placeholder="123 Main St, City"
              required
            />
          </div>

          <div class="register-form-row">
            <div class="register-form-group">
              <label for="password">Password</label>
              <input
                type="password"
                id="password"
                name="password"
                placeholder="Min 6 characters"
                required
              />
            </div>
            <div class="register-form-group">
              <label for="confirmPassword">Confirm Password</label>
              <input
                type="password"
                id="confirmPassword"
                name="confirmPassword"
                placeholder="Re-enter password"
                required
              />
            </div>
          </div>

          <div class="register-form-group">
            <label for="userImage">Profile Picture (Optional)</label>
            <input
              type="file"
              id="userImage"
              name="userImage"
              accept="image/*"
            />
            <small>Accepted: JPG, PNG, GIF (Max 5MB)</small>
          </div>

          <button type="submit" class="register-btn">Register</button>

          <div class="register-login-link">
            Already have an account? <a href="/login">Login here</a>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
